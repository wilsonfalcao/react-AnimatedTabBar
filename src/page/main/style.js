import { StyleSheet } from 'react-native';

export const mainStyle = StyleSheet.create({
    container: {
      flex: 1,
      backgroundColor: '#fff',
      alignItems: 'center',
      justifyContent: 'center',
    },
    textAnimated:{
      fontSize:20,
      fontWeight:"bold",
      color:"rgb(40,224,70)",
      marginBottom:50,
    }
});